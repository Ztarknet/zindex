apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.labels.zindex.name }}
  labels:
    app: {{ .Values.labels.zindex.name }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.ports.zindex }}
  selector:
    app: {{ .Values.labels.zindex.name }}
